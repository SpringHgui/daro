// 本文件由 tool/gen_version.py 从 pubspec.yaml 自动生成,请勿手工修改。
// 改版本只改 pubspec.yaml 的 version:,然后运行:
//   python tool/gen_version.py
// 一致性由 test/app_version_test.dart 守护(漂移会让 CI 失败)。

/// 语义版本号(不含 build 号),例如 `0.1.0`。
const String kAppVersion = '0.8.1';

/// pubspec.yaml 里 `+` 后面的构建号,例如 `1`。
const String kAppBuild = '13';

/// 完整版本串,例如 `0.1.0+1`。
const String kAppVersionFull = '0.8.1+13';
